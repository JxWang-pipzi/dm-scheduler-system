server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dm_scheduler?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 030817
    driver-class-name: com.mysql.cj.jdbc.Driver
  
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.dm.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.example.dm.mapper: debug
